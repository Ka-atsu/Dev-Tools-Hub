"use client";
import { Base64Tool } from "@/src/features/base64/components/Base64Tool";

export default function Base64Page() {
  return (
    <div style={{ maxWidth: "600px" }}>
      <Base64Tool />
    </div>
  );
}
